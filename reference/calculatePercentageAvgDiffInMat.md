# General function for calculating percentage difference of average column means in matrix

General function for calculating percentage difference of average column
means in matrix

## Usage

``` r
calculatePercentageAvgDiffInMat(targetMat)
```

## Arguments

- targetMat:

  Matrix for which column means should be compared

## Value

percDiffVector Vector with percentage difference, where first element
always will be 100
