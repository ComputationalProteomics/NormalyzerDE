# Generate first page in output report and write to viewport

Generate first page in output report and write to viewport

## Usage

``` r
plotFrontPage(currentjob, currentFont)
```

## Arguments

- currentjob:

  Name of current run.

- currentFont:

  Font used for output document.

## Value

None
