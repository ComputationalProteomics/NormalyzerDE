# Create directory, or return error if already present

Create directory, or return error if already present

## Usage

``` r
createDirectory(targetPath)
```

## Arguments

- targetPath:

  Path where to attempt to create directory

## Value

None
