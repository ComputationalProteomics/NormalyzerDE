% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diann.R
\name{diannInputOptions}
\alias{diannInputOptions}
\title{Create validated DIA-NN input options}
\usage{
diannInputOptions(
  level = c("auto", "protein", "precursor"),
  sep = "\\t",
  sampleCol = NULL,
  featureCol = NULL,
  quantityCol = NULL,
  extraCols = NULL,
  decoy = TRUE,
  qEnable = TRUE,
  qCols = NULL,
  qCutoffs = 0.01,
  minPositive = 0,
  rt = NULL
)
}
\arguments{
\item{level}{One of `"auto"`, `"protein"`, or `"precursor"`. If `"auto"`,
NormalyzerDE tries to infer whether the DIA-NN file is protein- or
precursor-level.}

\item{sep}{Field separator for DIA-NN TSV files.}

\item{sampleCol}{Optional DIA-NN report sample column name. If `NULL`,
NormalyzerDE uses `"Run"` or `"File.Name"` when present.}

\item{featureCol}{Optional DIA-NN report feature column name.}

\item{quantityCol}{Optional DIA-NN report quantity column name.}

\item{extraCols}{Optional character vector of additional DIA-NN columns to
carry along as row annotation.}

\item{decoy}{Whether to filter out decoys when the DIA-NN report includes a
`"Decoy"` column.}

\item{qEnable}{Whether to filter rows by q-values when available.}

\item{qCols}{Character vector of q-value columns to use. Use `NULL` or
`"auto"` to select reasonable defaults, or `"none"` to disable q-value
filtering.}

\item{qCutoffs}{Numeric vector of q-value thresholds. If a single value is
provided it is recycled to match `qCols`.}

\item{minPositive}{Minimum positive value threshold. Non-zero values below
this are converted to `NA` before analysis.}

\item{rt}{Retention time column configuration. Use `NULL` (default behavior),
`FALSE` to disable, `TRUE` to enable default `"RT"`, a character column
name, or `list(col = "<name>")`.}
}
\value{
A list suitable to pass as `inputOptions`.
}
\description{
Helper to construct an `inputOptions` list for `inputFormat = "diann"`. This
keeps DIA-NN-specific parameters out of the main entrypoint signatures while
still providing a typo-safe, validated API.
}
\examples{
diannInputOptions(
  level = "precursor",
  quantityCol = "Precursor.Quantity",
  qCols = "Q.Value",
  qCutoffs = 0.01,
  rt = "RT"
)
}
