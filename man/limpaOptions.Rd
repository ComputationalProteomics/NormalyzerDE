% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/limpa_utils.R
\name{limpaOptions}
\alias{limpaOptions}
\title{Create validated limpa options}
\usage{
limpaOptions(
  proteinIdCol = "auto",
  byRow = FALSE,
  dpc = NULL,
  dpcMethod = c("none", "dpc", "dpcON", "dpcCN"),
  dpcArgs = NULL,
  quantArgs = NULL,
  quantifiedRds = NULL,
  deArgs = NULL,
  keep = c("none", "elist", "fit", "all"),
  postQuantNorm = c("none", "GI", "median", "mean", "Quantile", "CycLoess", "RLR",
    "quantile")
)
}
\arguments{
\item{proteinIdCol}{Optional protein identifier column used for summarizing
peptide/precursor rows to proteins. Use `"auto"` (default) to infer a
common identifier, or `NULL` to keep each row separate.}

\item{byRow}{Whether to quantify each row separately with
`limpa::dpcQuantByRow()`.}

\item{dpc}{Optional DPC parameters to pass to `limpa`.}

\item{dpcMethod}{DPC estimation method when `dpc` is not supplied.}

\item{dpcArgs}{Optional named list of extra arguments for DPC estimation.}

\item{quantArgs}{Optional named list of extra arguments for
`limpa::dpcQuant()` / `limpa::dpcQuantByRow()`.}

\item{quantifiedRds}{Optional path to a quantified `EList` RDS file for
reuse with `normalyzerDE(type = "limpa")`.}

\item{deArgs}{Optional named list of extra arguments for `limpa::dpcDE()`.}

\item{keep}{Which intermediate `limpa` objects to retain in
`backendData(nst)$limpa`.}

\item{postQuantNorm}{Optional between-sample normalization applied after
quantification and before `limpa::dpcDE()`.}
}
\value{
A validated list suitable to pass as `limpaOptions`.
}
\description{
Helper to construct a grouped options object for `preQuant = "limpa"` and
`type = "limpa"` workflows.
}
\examples{
limpaOptions(
  byRow = TRUE,
  quantArgs = list(chunk = 1000L, verbose = FALSE)
)
}
